# Cognivest — backend runtime dependencies.
#
# NOTE: `pyproject.toml` is the SOURCE OF TRUTH for dependencies. This file
# mirrors the runtime (non-dev) deps for environments/tooling that consume a
# plain requirements file (e.g. some Docker base images or pip caches).
# Keep it in sync with `[project.dependencies]` in pyproject.toml.

fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0
pydantic>=2.6,<3.0
pydantic-settings>=2.2,<3.0
sqlalchemy[asyncio]>=2.0,<3.0
asyncpg>=0.29,<1.0
alembic>=1.13,<2.0
celery[redis]>=5.3,<6.0
redis>=5.0,<6.0
cognee[fastembed]==1.2.2
python-jose[cryptography]>=3.3,<4.0
passlib[bcrypt]>=1.7,<2.0
httpx>=0.27,<1.0
structlog>=24.1
yfinance>=0.2,<1.0        # keyless market-data vendor for price history (issue #8)
